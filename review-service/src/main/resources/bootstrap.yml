spring:
  application:
      name: review-service
  config:
      name: review-service
  cloud:
      config:
        uri: ${CONFIG_SERVER_URL:http://localhost:8888}
        password: ${CONFIG_SERVER_PASSWORD:12345}
        username: user
#        fail-fast: true
